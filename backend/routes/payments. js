const express = require("express");
const router = express.Router();

// Placeholder for payment API
router.get("/", (req,res)=>{
  res.json({msg:"Payments API working"});
});

module.exports = router;
